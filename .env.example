# Supabase — opcional. Sin estas variables la app corre 100% local (IndexedDB)
# y lo indica en la pantalla "Mi viaje". Son claves públicas: nunca pongas acá
# la service_role key, que salta el RLS.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
